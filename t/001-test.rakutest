use v6;
use lib 'lib';
use File::Path::Copy;
use Test;

plan 2;

ok(emptypath('scratch'.IO), "empting scratch");
#run qqww{exa -F -lahigHTb --colour-scale --time-style=full-iso --icons=always scratch};

ok(copypath("rakulib".IO, "scratch".IO), "copying the whole rakulib tree into scratch");
#run qqww{exa -F -lahigHTb --colour-scale --time-style=full-iso --icons=always scratch};

